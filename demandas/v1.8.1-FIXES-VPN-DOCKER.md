# v1.8.1 - Fixes: VPN, Docker, Backend Dependencies

## Data: 2026-05-27

## Resumo
Correções de infraestrutura Docker, VPN e dependências do backend.

## Alterações

### Docker & VPN
- Fix: VPN healthcheck mudado de `ping 8.8.8.8` para `ping 10.0.0.18` (VPN bloqueia ICMP externo)
- Fix: Docker socket montado no backend para gerenciamento do container VPN
- Fix: VITE_API_URL removido (usa proxy do Vite ao invés de localhost:3030)
- Fix: `tailwind.config.ts` e `postcss.config.js` montados no container frontend

### Backend
- Fix: `pdfkit` e `nodemailer` adicionados ao package.json permanentemente
- Fix: Backend agora detecta estado do container VPN corretamente

### Frontend (UI Polish)
- Fix: Botão de texto branco em backgrounds coloridos
- Fix: Badges com variantes light/dark mode
- Fix: QueryPage toolbar overflow para dropdown visível
- Fix: Card shadows (shadow-sm, hover:shadow-md)
- Add: CSS modular (`buttons.css`, `badges.css`, `cards.css`)

### Problema Conhecido
- Credenciais das conexões SQL Server foram encriptadas com uma DBA_MASTER_KEY diferente da atual
- Solução: recriar as conexões pela interface ou restaurar a chave original

## Commits
- `1ababa9` - fix: UI polish + docker config fixes + backend deps
- `a8a29ff` - fix: VPN healthcheck + backend deps (pdfkit, nodemailer)
- `0de5070` - fix: mount docker socket in backend for VPN container management
