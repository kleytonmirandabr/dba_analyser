services:
  vpn:
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
  backend:
    volumes:
      - ./backend/src:/app/src
      - vpn-data:/vpn
      - /var/run/docker.sock:/var/run/docker.sock
