FROM node:22-alpine

# Install docker CLI for VPN container management
RUN apk add --no-cache docker-cli

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY tsconfig.json ./
COPY src ./src

EXPOSE 3030

CMD ["npm", "run", "dev"]
